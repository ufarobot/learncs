---
const { eyebrow, title, text, className = '' } = Astro.props;
---
<div class:list={['section-head', 'section-intro', className]}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  {text && <p>{text}</p>}
</div>
