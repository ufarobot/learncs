---
import { readNavigatorHtml } from '../../lib/navigator-static.js';

Astro.response.headers.set('content-type', 'text/html; charset=utf-8');

const html = await readNavigatorHtml();
---

<Fragment set:html={html} />
